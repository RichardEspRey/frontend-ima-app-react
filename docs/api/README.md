# Referencia de API generada

Este archivo se genera con `npm run docs:api` a partir de los bloques JSDoc de
`src/shared/`, `src/entities/` y `src/features/`. **No lo edites a mano** — edita el JSDoc.

Está vacío porque esas carpetas todavía no existen: se crean en el incremento 2 del
refactor. La cadena de generación ya está verificada y funcionando.
